import { NextResponse } from "next/server";import { cookies } from "next/headers";import { z } from "zod";import { verifyToken } from "@/lib/auth";import { db } from "@/lib/db";
const schema=z.object({name:z.string().min(2).optional(),email:z.email().optional(),branchId:z.string().optional(),active:z.boolean().optional()});async function admin(){const u=await verifyToken((await cookies()).get("erp_session")?.value);return u?.role==="ADMIN"?u:null}
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){if(!await admin())return NextResponse.json({error:"Forbidden"},{status:403});const body=schema.safeParse(await request.json().catch(()=>null));if(!body.success)return NextResponse.json({error:"Invalid employee"},{status:400});const {id}=await params;return NextResponse.json(await db.user.update({where:{id},data:body.data,select:{id:true,name:true,email:true,active:true,branchId:true}}))}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){if(!await admin())return NextResponse.json({error:"Forbidden"},{status:403});const {id}=await params;return NextResponse.json(await db.user.update({where:{id},data:{active:false},select:{id:true,active:true}}))}

