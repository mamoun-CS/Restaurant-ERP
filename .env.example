DATABASE_URL="postgresql://postgres:postgres@localhost:5432/restaurant_erp?schema=public"
AUTH_SECRET="replace-with-at-least-32-random-characters"
APP_URL="http://localhost:3000"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
# Comma-separated production domains allowed to call protected API routes.
# Example: ALLOWED_ORIGINS="https://your-domain.com,https://www.your-domain.com"
ALLOWED_ORIGINS="http://localhost:3000"

SMTP_HOST=
SMTP_PORT=
SMTP_SECURE=
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_NAME=
SMTP_FROM_EMAIL=
